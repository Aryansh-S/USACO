//helper function if needed to balance the tree for binary search, etc.

int upper_power_of_two(int v) {
    --v;
    v |= v >> 1;
    v |= v >> 2;
    v |= v >> 4;
    v |= v >> 8;
    v |= v >> 16;
    return ++v;
}
